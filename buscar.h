#ifndef BUSCAR_H
#define BUSCAR_H
#include <iostream>
#include <vector>
#include "red.h"
using namespace std;

int stringtoint(string numero);
bool comprobar(string buscar,string caracter);
bool compvector (const vector < string > &algo, string palabra);

#endif // BUSCAR_H
